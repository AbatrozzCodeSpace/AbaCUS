#ifndef MESH_SPLIT_HPP
#define MESH_SPLIT_HPP

#include <AbaCUS/ds/Graph.hpp>

#ifdef CINDER_CINDER
#include <cinder/TriMesh.h> 
#endif

namespace abacus {
	namespace mesh {
		
		class MeshSplit {
		public :
			
		private:
		
		};

		inline bool split(  );

	};
	using namespace mesh;
};

#endif